import { describe, it, expect } from 'vitest';

describe('Example Unit Test', () => {
  it('should pass basic test', () => {
    expect(1 + 1).toBe(2);
  });

  it('should verify monorepo structure', () => {
    expect(true).toBe(true);
  });
});